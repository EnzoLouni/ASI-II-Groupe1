export default class User {
    constructor({id, nom, prenom, age, mail}) {
        this.id = id;
        this.nom = nom;
        this.prenom = prenom;
        this.age = age;
        this.mail = mail;
    }
}
